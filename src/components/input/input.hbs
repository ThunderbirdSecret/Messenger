<input
    class="{{class}}"
    type="{{type}}"
    placeholder="{{placeholder}}"
    value="{{value}}"
    name="{{name}}"
    {{status}}
    id="{{id}}"
    accept="{{accept}}"
    onSubmit="return false"
/>
