<div id="{{id}}" class="hidden flex flex-col gap-y-10 p-[20px] items-center bg-zinc-700 rounded-lg z-10 absolute inset-60 w-[350px] h-[300px] overflow-y-scroll scrollbar shadow-lg">
    <div class="ml-auto text-white" id="modal-el">
        {{{close}}}
    </div>
            <h3 id="text_modal" class="text-center">{{text}}</h3>
        {{#if inputId}} 
            {{{input}}}
        {{/if}}
        {{#if users}}
            {{{list}}}
        {{/if}}
        {{#if btn}}
    <div class="flex text-sm">
        {{{button}}}
    </div>{{/if}}
</div>
